first-time environment setup (linux):
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

what is not included:
checkpoints
input data
    RGB input in input/images/
    ground truth mask in input/masks